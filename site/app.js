/**
 * Silverstripe Mods - Client-side JavaScript
 * Minimal client-side functionality for the static site
 *
 * This file is largely unnecessary as the site uses static HTML generation
 * with all module data embedded directly into the HTML at build time.
 * Kept for potential future enhancements or client-side interactions.
 */

// Simple console message to confirm the site has loaded successfully
console.log('Silverstripe Mods - Static site loaded successfully!');
